//! About tab — native egui slot (links, update check, danger zone).

use dioxus_egui::dioxus::prelude::*;

#[component]
pub fn AboutTab() -> Element {
    rsx! {
        div { "native": "egui" }
    }
}
