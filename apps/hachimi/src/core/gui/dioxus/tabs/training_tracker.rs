//! Training Tracker tab — native egui slot (in-core module tab registry).

use dioxus_egui::dioxus::prelude::*;

#[component]
pub fn TrainingTrackerTab() -> Element {
    rsx! {
        div { "native": "egui" }
    }
}
