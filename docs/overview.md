# Project Overview — HachimiRedux

HachimiRedux is a game enhancement mod for **The Honse Game** (Steam). It works by DLL proxying (`cri_mana_vpx.dll`) and hooking IL2CPP methods at runtime. It has a plugin system where external DLLs (cdylib) interact via a C ABI vtable.
